#include <stdio.h>

int main() {
    // arithmetic operators = + - * / % ++ --

    int x = 2;
    int y = 3;
    int z = 0;

    z = x + y; // Addition
    printf("Addition: %d + %d = %d\n", x, y, z);

    return 0;
}